# Secure Access Backend (Node.js)

## Requirements
- Node.js
- MySQL (phpMyAdmin)

## Setup
1) Create database and import SQL (schema/dump).
2) Create `.env` file (copy from `.env.example` and fill values).

## Install
```bash
npm install
